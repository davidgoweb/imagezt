version: '3.8'

services:
  imagezt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: imagezt
    ports:
      - "${PORT:-5930}:5930"
    environment:
      - NODE_ENV=development
      - PORT=5930
      - HOST=0.0.0.0
      - CORS_ENABLED=false
      - RATE_LIMIT_ENABLED=true
      - RATE_LIMIT_WINDOW=900000
      - RATE_LIMIT_MAX=100
      - DEBUG=false
      - VERBOSE_ERRORS=false
      - LOG_LEVEL=debug
      - HEALTH_CHECK_ENABLED=true
      - HEALTH_CHECK_PATH=/health
    volumes:
      - ./.env.example:/app/.env:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:5930/health', (res) => { process.exit(res.statusCode === 200 ? 0 : 1) })"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s